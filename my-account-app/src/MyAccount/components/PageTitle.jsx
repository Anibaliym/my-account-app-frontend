export const PageTitle = ({ titleDescription }) => {
    return (
        <p className="page-title animate__animated animate__fadeInDown animate__faster">
            { titleDescription }
        </p>
    )
}
